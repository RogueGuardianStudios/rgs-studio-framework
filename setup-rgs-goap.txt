INSTRUCTIONS FOR CLAUDE CODE — rgs-goap repo setup

You are in the rgs-goap repository. Your job is to link this
project to the rgs-studio-framework using a git submodule,
then verify the setup works.

== STEP 1: Add the framework as a submodule ==

git submodule add https://github.com/RogueGuardianStudios/rgs-studio-framework.git rgs-studio-framework
git submodule update --init

== STEP 2: Copy and customize the bootstrap CLAUDE.md ==

cp rgs-studio-framework/templates/PROJECT_BOOTSTRAP_CLAUDE.md ./CLAUDE.md

Then edit ./CLAUDE.md and replace every instance of
[PROJECT_NAME] with rgs-goap

There should be one occurrence on this line:
  9. rgs-studio-framework/projects/[PROJECT_NAME]/CLAUDE.md
Change it to:
  9. rgs-studio-framework/projects/rgs-goap/CLAUDE.md

== STEP 3: Verify all paths resolve ==

After setup, confirm that every file listed in the bootstrap
CLAUDE.md actually exists in the submodule. The full list is:

  rgs-studio-framework/values.md
  rgs-studio-framework/CLAUDE.md
  rgs-studio-framework/agents/orchestrator.md
  rgs-studio-framework/state/active-project.md
  rgs-studio-framework/state/decisions.md
  rgs-studio-framework/state/open-questions.md
  rgs-studio-framework/state/agent-status.md
  rgs-studio-framework/state/known-issues.md
  rgs-studio-framework/projects/rgs-goap/CLAUDE.md

Run: ls -la <path> for each one. Every file must exist.
If any file is missing, stop and report which ones.

== STEP 4: Commit ==

git add .gitmodules rgs-studio-framework CLAUDE.md
git commit -m "Add rgs-studio-framework submodule and bootstrap CLAUDE.md"

== STEP 5: Report results ==

Tell the user:
- Whether all 9 files resolved successfully
- Whether the commit succeeded
- The contents of the final CLAUDE.md so the user can review it

== NOTES ==

- The framework repo is: https://github.com/RogueGuardianStudios/rgs-studio-framework.git
- The submodule should point to the main branch by default
- Do NOT modify any files inside the rgs-studio-framework/ submodule
- The only new file in rgs-goap is ./CLAUDE.md at the repo root
